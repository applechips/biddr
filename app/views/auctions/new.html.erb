<h1>Create New Auction</h1>

<%= simple_form_for @auction do |f| %>
    <%= f.input :title, label: "Auction Item" %>
    <%= f.input :details %>
    <%= f.input :ends_on, :as => :datetime_picker %>
    <%= f.input :reserve_price %>
    <%= f.button :submit %>
<% end %>
